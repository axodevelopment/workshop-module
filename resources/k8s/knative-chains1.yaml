apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: validate-transaction
spec:
  broker: default
  filter:
    attributes:
      type: defaultChain
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: mizuho-workshop-4